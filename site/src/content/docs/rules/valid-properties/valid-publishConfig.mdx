---
title: 'valid-publishConfig'
description: 'Enforce that the `publishConfig` property is valid.'
---

💼 This rule is enabled in the following configs: ✅ `recommended`, 📦 `recommended-publishable`.

{/* end auto-generated rule header */}

import { Tabs, TabItem } from '@astrojs/starlight/components';

This rule uses [`package-json-validator`](https://github.com/michaelfaith/package-json-validator) to validate the `publishConfig` property, if present, against the following criteria:

- It must be an object
- Each property in the object that maps to an existing package.json property (e.g. `exports`) must pass the same validations that would be applied to the top-level property
- For properties that don't map to other top-level package.json properties (e.g. `access`), they have their own set of validations that are applied, based on a combination of the `npm` and `pnpm` specifications

## Examples

<Tabs>
<TabItem label="❌ Incorrect">

```json
{
  "publishConfig": {
    "provenance": "true"
  }
}
```

</TabItem>
<TabItem label="✅ Correct">

```json
{
  "publishConfig": {
    "provenance": true
  }
}
```

</TabItem>
</Tabs>

## Related Rules

- [`require-publishConfig`](/rules/require-properties/require-publishconfig) - Enforces that the `publishConfig` property is present.

## Further Reading

- [npm: package.json > publishConfig](https://docs.npmjs.com/cli/v11/configuring-npm/package-json#publishconfig)
- [pnpm: package.json > publishConfig](https://pnpm.io/package_json#publishconfig)
