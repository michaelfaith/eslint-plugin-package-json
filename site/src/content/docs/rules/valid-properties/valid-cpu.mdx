---
title: 'valid-cpu'
description: 'Enforce that the `cpu` property is valid.'
---

💼 This rule is enabled in the ✅ `recommended` config.

{/* end auto-generated rule header */}

import { TabItem, Tabs } from '@astrojs/starlight/components';

The rule checks that, if present, the `cpu` property is an Array of non-empty strings.

## Examples

<Tabs>
<TabItem label="❌ Incorrect">

```json
{
  "cpu": true
}
```

</TabItem>
<TabItem label="✅ Correct">

```json
{
  "cpu": ["x64", "ia32"]
}
```

</TabItem>
</Tabs>

## Related Rules

- [`require-cpu`](/rules/require-properties/require-cpu) - Enforces that the `cpu` property is present.

## Further Reading

- [npm: package.json > cpu](https://docs.npmjs.com/cli/v11/configuring-npm/package-json#cpu)
