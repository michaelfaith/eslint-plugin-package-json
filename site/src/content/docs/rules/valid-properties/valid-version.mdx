---
title: 'valid-version'
description: 'Enforce that the `version` property is valid.'
---

💼 This rule is enabled in the following configs: ✅ `recommended`, 📦 `recommended-publishable`.

{/* end auto-generated rule header */}

import { TabItem, Tabs } from '@astrojs/starlight/components';

This rule uses [`package-json-validator`](https://github.com/michaelfaith/package-json-validator) to validate the `version` property, if present, against the following criteria:

- It must be a string
- It should pass [`semver`](https://www.npmjs.com/package/semver) validation

## Examples

<Tabs>
<TabItem label="❌ Incorrect">

```json
{
  "version": "1"
}
```

</TabItem>
<TabItem label="✅ Correct">

```json
{
  "version": "1.2.3"
}
```

</TabItem>
</Tabs>

## Related Rules

- [`require-version`](/rules/require-properties/require-version) - Enforces that the `version` property is present.

## Further Reading

- [npm: package.json > version](https://docs.npmjs.com/cli/v11/configuring-npm/package-json#version)
