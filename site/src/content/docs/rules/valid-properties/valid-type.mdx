---
title: 'valid-type'
description: 'Enforce that the `type` property is valid.'
---

💼 This rule is enabled in the following configs: ✅ `recommended`, 📦 `recommended-publishable`.

{/* end auto-generated rule header */}

import { TabItem, Tabs } from '@astrojs/starlight/components';

This rule uses [`package-json-validator`](https://github.com/michaelfaith/package-json-validator) to validate the `type` property, if present, against the following criteria:

- It must be a string
- The value must be either `commonjs` or `module`

## Examples

<Tabs>
<TabItem label="❌ Incorrect">

```json
{
  "type": "esm"
}
```

</TabItem>
<TabItem label="✅ Correct">

```json
{
  "type": "module"
}
```

</TabItem>
</Tabs>

## Related Rules

- [`require-type`](/rules/require-properties/require-type) - Enforces that the `type` property is present.

## Further Reading

- [Node.js: Packages > type](https://nodejs.org/api/packages.html#type)
