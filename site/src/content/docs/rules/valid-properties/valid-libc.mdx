---
title: 'valid-libc'
description: 'Enforce that the `libc` property is valid.'
---

💼 This rule is enabled in the following configs: ✅ `recommended`, 📦 `recommended-publishable`.

{/* end auto-generated rule header */}

import { TabItem, Tabs } from '@astrojs/starlight/components';

The rule checks that, if present, the `libc` property is a string or an Array of strings.

## Examples

<Tabs>
<TabItem label="❌ Incorrect">

```json
{
  "libc": {
    "glibc": true
  }
}
```

</TabItem>
<TabItem label="✅ Correct">

```json
{
  "libc": ["glibc", "musl"]
}
```

```json
{
  "libc": "glibc"
}
```

</TabItem>
</Tabs>

## Related Rules

- [`require-libc`](/rules/require-properties/require-libc) - Enforces that the `libc` property is present.

## Further Reading

- [npm: package.json > libc](https://docs.npmjs.com/cli/v11/configuring-npm/package-json#libc)
