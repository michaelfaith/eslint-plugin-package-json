---
title: 'valid-bugs'
description: 'Enforce that the `bugs` property is valid.'
---

💼 This rule is enabled in the following configs: ✅ `recommended`, 📦 `recommended-publishable`.

{/* end auto-generated rule header */}

import { TabItem, Tabs } from '@astrojs/starlight/components';

This rule uses [`package-json-validator`](https://github.com/michaelfaith/package-json-validator) to validate the `bugs` property, if present, against the following criteria:

- It must be either a `string` or an `object`
- If it's a `string`, it should be a valid URL
- If it's an `object`, it should have either `email` or `url` (or both)
- `email` should be a valid email address
- `url` should be a valid URL

## Examples

<Tabs>
<TabItem label="❌ Incorrect">

```json
{
  "bugs": "some@email.com"
}
```

</TabItem>
<TabItem label="✅ Correct">

```json
{
  "bugs": {
    "email": "support@nin.com",
    "url": "https://nin.com/support"
  }
}
```

</TabItem>
</Tabs>

## Related Rules

- [`require-bugs`](/rules/require-properties/require-bugs) - Enforces that the `bugs` property is present.

## Further Reading

- [npm: package.json > bugs](https://docs.npmjs.com/cli/v11/configuring-npm/package-json#bugs)
